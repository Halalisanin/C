#include <stdio.h>

/**
 *
 * main - Entry point of the program
 *
 * Description: This program shows the use of variables
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int age = 18;
printf("age (before): %d\n", age);

age = 25;
printf("age (after): %d\n", age);

return (0);
}
